

#include <stdio.h>

int main()
{
    int i, n, count=0;
    printf("enter a number: ");
    scanf("%d", &n);
    
    if(n==0)
    printf("no of digits = 0");
    
    while(n!=0)
    {
        n=n/10;
        count++;
    }
    
    printf("no of digits = %d", count); 

}
